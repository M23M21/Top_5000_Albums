<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Users</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Users</h1>

  <a href="/" class="home-button">Home</a>

  <table class="user-table">
    <tr>
      <th>User Name</th>
      <th>Email</th>
      <th>Password</th> 
    </tr>
    <% users.forEach(user => { %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><%= user.password %></td>
      </tr>
    <% }); %>
  </table>
</body>
</html>
